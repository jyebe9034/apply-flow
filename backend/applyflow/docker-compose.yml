version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: applyflow-postgres
    restart: always
    environment:
      POSTGRES_DB: applyflow
      POSTGRES_USER: applyuser
      POSTGRES_PASSWORD: 7pGA9021@
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: